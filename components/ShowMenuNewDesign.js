import * as React from "react";
import { StatusBar, StyleSheet } from "react-native";

const ShowMenuNewDesign = () => {
  return <StatusBar barStyle="default" />;
};

const styles = StyleSheet.create({});

export default ShowMenuNewDesign;
